node_modules/
dist/
coverage/

# Generated by the conformance spec from the vendored fonts — derived data, and
# regenerating it is one command. Committing it would let a stale expectation
# outlive the fonts it was computed from.
.conformance/
